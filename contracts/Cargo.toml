[package]
name = "oracle_contracts"
version = "0.1.0"
edition = "2024"

[dependencies]
smplx-std = "0.0.9"

[dev-dependencies]
anyhow = "1"
# Storm Tree construction and witness packing, shared with the network side.
storm-tree = { path = "../storm-tree" }
monotree = "0.4.0"
thiserror = "2"
