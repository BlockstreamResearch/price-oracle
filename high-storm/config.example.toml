
[service]
port = 9000
ipc_path = "/tmp/high-storm.sock"
external_api_address = "127.0.0.1:9001"

[service.signer]
# secpr256k1 32-byte hex-encoded private key, used for signing,
# this is a example key, do not use it anywhere else
private_key = "804622cda0d8e634317a12651d91751ceff5c081f2b5f63ef7912725c7275e5d"

[service.elements_rpc]
url = "http://127.0.0.1:18884"
username = "high-storm"
password = "high-storm"
wallet = "funded-key"

[service.user_requests]
# Protocol policy values; the architecture specification intentionally does not fix amounts.
operational_fee_sats = 1000
tick_burn_reserve_sats = 1000
issuance_transaction_fee_sats = 1000

[service.db]
url = "localhost:5432"
username = "high-storm"
password = "high-storm"
database = "high-storm"
max_connections = 5
